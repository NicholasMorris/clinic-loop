# Pinned versions for the text-to-speech spike worker.
# These are recorded to ensure reproducible, offline measurement runs.

[pins]
# Native transformers implementation (no fork)
fork_commit = "native"
transformers_version = "5.17.0"
torch_version = "2.14.0"
diffusers_version = "0.40.0"
model_id = "vibevoice/VibeVoice-1.5B-hf"
