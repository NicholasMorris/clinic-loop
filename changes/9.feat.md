LLM configuration and factory (#9).

- Add `models.toml` as the single source of truth for all local model configuration (primary, judge, fallback, fake roles).
- Implement `load_models_config()` to load and validate TOML with loopback constraint, pinning rules, and required role enforcement.
- Implement `build_chat_model(role)` factory to create ChatOpenAI instances from config rows, decoupling agent code from model selection.
- Implement `structured_output()` with fallback from tool calling to JSON schema on first failure.
- Implement `FakeChatModel` for offline testing of agent logic with scripted responses and no network access.
- Add conformance tests for L2 (loopback constraint) and L7 (model swapping without code changes).
- Ensure all tests pass with pytest-socket blocking non-loopback connections.
