Escalation detection engine for adverse events, misuse, distress, pregnancy, and clinical advice. Deterministic rules (pattern matching on normalized text) union with optional classifier; priority-based category resolution. EscalationClear token minted only by detector, required for draft entry point. Reachability helper validates graph structure prevents escalated threads from reaching draft. Development-set fixture with 60+ synthetic cases. Routing (queue and response time) read from jurisdiction ruleset with validation.
