Add triage evaluation gate: hard gates for escalation_recall and rule_violation_rate, baseline regression detection for intent_accuracy and draft_acceptance_rate_proxy, threshold loosening check, and first-baseline handling. Gate runs on every PR via `make ci` and blocks merge on regression.
