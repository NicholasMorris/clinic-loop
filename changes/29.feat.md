Guard boundary FastAPI dependency and exception handler enforce compliance rules on every POST /messages before storage. Blocked messages return HTTP 422 with rule_ids and match offsets; allowed messages return 201 with text_sha256 and jurisdiction_source header. Explicit uk or nz jurisdiction returns 501 RulesetNotImplemented; unset jurisdiction defaults to AU strict with jurisdiction_source flag. Guard runs for all callers, including generated code.
