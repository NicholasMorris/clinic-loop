SimClinic world foundation: entities, deterministic generator, and regime parameters.

Add `clinicloop.world` package with frozen pydantic models for Patient, Questionnaire, Consult, Prescription, Order, and Message. All entities carry an explicit `synthetic: Literal[True]` field enforcing synthetic-only status (R6).

Implement deterministic world generator with stream isolation: `generate_world(seed, population_size, span_days)` uses separate numpy.random.Generator streams for each entity type, guaranteeing reproducibility across processes regardless of PYTHONHASHSEED. World snapshot artifact carries schema_version, seed, population_size, and span_days; reading and re-exporting yields identical SHA-256 digests.

Add regime parameter registry for AU, NZ, UK. AU is populated with operational parameters (flat shipping $9.95, free shipping threshold $129, 2-business-day termination cutoff, 3-day damage window, 6-month consultation validity, 4–5 working day delivery, 1-business-day dispatch commitment) plus recorded x-01 conflict noting the inventory's disagreement on delivery time. NZ and UK are placeholder regimes that raise RegimeParameterNotSet on any parameter access, enforcing explicit seam semantics (R5).

Add fictional identifier ranges (phone 0400–0499, email test.example.com, health ID prefix 99) for synthetic data integrity. Hand-transcribed KNOWN_INVENTORY_IDS manifest (ps-03, ps-04, po-01, po-02, po-06, x-01) verified by orchestrator at review time against docs/problem-inventory.md.

Requirement traceability: B2, C0, G1, R6 (synthetic-only), R5 (placeholder seam).
