AgentPort protocol and PortRegistry for SimClinic agent integration.

Introduces a protocol for replacing human worker steps with agents, a registry managing three toggleable scopes (triage, integrity, consult_documentation), and FakeAgentPort as a shipped demo double. Enables toggling agents on and off while preserving determinism and queue semantics.
