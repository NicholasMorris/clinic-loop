Hardware tier detection and setup. Detect available system memory and select an appropriate LLM model (64GB+, 48GB, 32GB, or 16GB tier). Check for external binaries (ffmpeg, whisper.cpp) and install ffmpeg if missing. Write model selection to configuration file. `make doctor` reports system configuration; `make setup` installs dependencies.
