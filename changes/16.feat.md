Implement evaluation core framework (M0-12, #16)

- Add global metric registry with duplicate detection via `DuplicateMetric` exception
- Define per-case artifact schema with Pydantic; enforce unique case IDs within component
- Compute tree hash from git blob SHAs of eval-relevant paths; detect staleness when paths change
- Implement append-only rule for artifact files; raise `AppendOnlyViolation` on overwrite
- Add recompute script to validate committed aggregates against per-case data
- Add no-loosening check to prevent threshold regression against `origin/main`
- Implement cassette key as tuple of (model_id, prompt_hash, sample_index, seed)
- Scan checklist file for requirement IDs; map to tests via `@pytest.mark.checklist_id` marker
- Validate unmapped requirements against committed allowlist; fail on drift
- Add conformance test `test_E2` to register the evaluation framework requirement
- Document core infrastructure in `docs/evals/eval-core.md`
